{
  "name": "wisign",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev:controller": "npm run dev --workspace=packages/controller",
    "dev:player": "npm run start --workspace=packages/player"
  }
}
